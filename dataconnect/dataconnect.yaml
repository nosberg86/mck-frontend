specVersion: "v1beta"
serviceId: "mck-frontend"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "mck-frontend-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
